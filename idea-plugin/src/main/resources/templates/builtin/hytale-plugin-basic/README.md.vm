# $artifactId

Generated by HytaleDev.

## Build

#if($isGradle)
- `./gradlew build`

## Run (Gradle)

If install auto-detect fails, set `HYTALE_HOME` or pass `-PhytaleHome=/path/to/Hytale/.../game/latest` (see `gradle.properties`).

- `./gradlew hytaleDoctor`
- `./gradlew runHytaleServer`
- `./gradlew runHytaleServerDebug`
#elseif($isMaven)
- `mvn package`
#end

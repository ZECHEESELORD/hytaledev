# $artifactId

Generated by HytaleDev.

## Build

#if($isGradle)
- `./gradlew build`
#elseif($isMaven)
- `mvn package`
#end

## Configuration

The wizard fills these build properties:

#if($isGradle)
- `gradle.properties`:
  - `hytale.serverJar`: path to `HytaleServer.jar`
  - `hytale.assetsPath`: path to `Assets.zip`
#elseif($isMaven)
- `pom.xml`:
  - `hytale.serverJar`: path to `HytaleServer.jar`
  - `hytale.assetsPath`: path to `Assets.zip`
#end
